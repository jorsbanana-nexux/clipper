# OPTIONAL — enables multi-speaker diarization (v0.2).
# Heavy (pulls in torch). NOT part of requirements.txt to keep the base
# install light for low-spec machines. Only install if you need multi-speaker.
#
# torch versions vary by platform (CPU vs CUDA). Adjust the first two lines to
# your environment if pip cannot find a matching wheel.
torch==2.3.1
torchaudio==2.3.1
pyannote.audio==3.1.1
